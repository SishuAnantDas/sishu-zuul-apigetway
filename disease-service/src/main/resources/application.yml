server:
    port: 8084

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/rick_hospital_db
        username: root
        password: root
    jpa:
        database-platform: org.hibernate.dialect.MySQL5Dialect
        generate-ddl: true
        hibernate:
            ddl-auto: update
